import { StyleSheet } from "react-native";

const StyleBasicApp = StyleSheet.create({
  fab: {
    position: "absolute",
    margin: 16,
    right: 0,
    bottom: 0,
  },
});

export default StyleBasicApp;
